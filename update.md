# 地圖編輯器改善

## 摘要

本次更新依據 `Todo` 擴大地圖畫布、提高 PNG 匯出解析度，並修正圖表圖層、共享資料來源、圖層刪除及右側工作區操作。

## 地圖版面與 PNG 匯出

- 桌面版地圖與右側工作區約為 70%／30%；窄螢幕維持上下排列。
- 地圖畫布固定為 16:9，避免瀏覽與輸出使用不同長寬比。
- PNG 預設輸出 2560×1440；修改寬度時會自動換算 16:9 高度。
- 匯出期間依輸出倍率調整地圖縮放、圖表、地名與邊界，再於輸出後還原原始中心、縮放與尺寸。
- 匯出按鈕在處理期間停用，避免重複建立輸出工作。

## 圖層顯示與操作

- 人口與共享圖表使用各自的 Leaflet pane，並由 Leaflet 自動管理 pane 的 SVG renderer，確保 minichart 顯示在不透明 GeoJSON 底圖上方。
- 圖層清單由上至下對應地圖由上至下的顯示順序，並維持拖曳排序及顯示切換。
- 移除出生率與扶老比分級填色圖層；舊瀏覽器工作區內留下的相關 ID 會在載入時忽略。
- 保留實心區域底圖、區域顏色設定、人口圖表及可拖曳地名。
- 共享圖層會顯示「刪除」按鈕，確認後從伺服器 catalog 永久移除；CSV 圖層的原始檔也會一併刪除。
- 內建圖層只允許顯示、隱藏及排序，不提供永久刪除。

## 新增共享圖層

### 使用現有資料

地圖頁可直接從已彙整資料建立共享圖層，設定包含：

- 圖層名稱
- 年份
- 資料類型：年度指標或年齡結構
- 性別：全部、男、女
- 圖表類型：長條圖、圓餅圖、甜甜圈圖
- 年度指標：總人口、扶老比、出生率、自然增加率、年出生、年死亡

年度指標圖層以所選指標作為單一系列；年齡結構圖層以現有五歲年齡組作為多個系列。建立時會保存當下資料快照，不會因日後重新彙整而自動改變。

### CSV 上傳

既有 CSV 上傳方式維持不變：檔案必須使用 UTF-8、包含「里」欄位，其他欄位須為有限數值。舊 catalog 若沒有來源欄位，會自動視為 CSV 圖層。

## 工作區折疊

右側的圖層工作區、新增共享圖表圖層、區域色彩設定及 PNG 匯出設定均採獨立的展開／收合區塊。區塊預設展開，收合後只保留標題；狀態不跨頁面重新整理保存。

## API

### `POST /api/map-custom-layers/from-data`

從已彙整資料建立共享圖層：

```json
{
  "name": "114 年總人口",
  "chart_type": "bar",
  "year": 114,
  "data_type": "indicators",
  "gender": "全部",
  "metric": "總人口"
}
```

- `chart_type`：`bar`、`pie`、`donut`
- `data_type`：`indicators`、`age`
- `metric`：年度指標必填，年齡結構可省略
- 成功回傳 201；資料不存在回傳 404；選項組合不合法回傳 400 或 422

### `DELETE /api/map-custom-layers/{layer_id}`

永久刪除共享圖層。成功回傳 204，不存在回傳 404。

### Catalog 相容欄位

```json
{
  "source_type": "csv | processed_data",
  "source_meta": {
    "year": 114,
    "data_type": "indicators",
    "gender": "全部",
    "metric": "總人口"
  }
}
```

`source_file` 僅 CSV 圖層需要。

## 驗證

- 服務與 API 測試共 14 項，涵蓋 CSV 驗證、既有資料快照、來源 metadata、舊 catalog 相容、圖層刪除、來源檔清除及 API 回應。
- 驗證指令：`PYTHONPATH=. python3 -m unittest discover -s tests -v`
- 此功能沿用現有無登入模式；任何可存取地圖頁與 API 的使用者都能新增或刪除共享圖層。
